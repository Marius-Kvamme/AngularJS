
/*
app.controller("myCtrl", function($scope) {
    $scope.firstName = ["John", "asdf", "daa"];
    $scope.lastName= "Doe";
  });
  
-ng-init="myCol='lightblue'"
<div>
    {{ firstName[0] + " " + lastName }}
</div>
    <div ng-repeat="x in firstName">
{{x}}
    </div>
    {{ lastName | uppercase }}
    <li ng-repeat="x in names | orderBy:'country'">
    <li ng-repeat="x in names | filter : 'i'">
    
    app.filter('myFormat', function() {
    return function(x) {
        var i, c, txt = "";
        for (i = 0; i < x.length; i++) {
            c = x[i];
            if (i % 2 == 0) {
                c = c.toUpperCase();
            }
            txt += c;
        }
        return txt;
    };
});

$location sier urlen til siden.

*/